#include <iostream>
using namespace std;

// while() {...}, for() {...}, do {...} while(), nested loops
int main()
{
  /**
   * break    => statement
   * continue => statement
   * goto     => statement
   */

  /**
   * example infinite for loop
    for ( ; ; ) {
      printf("This loop will run forever\n");
    }
  */
  return 0;
}